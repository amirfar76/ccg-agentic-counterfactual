# Models

Trained artifacts (e.g., NPE/posterior models) should be written here.

This folder is ignored by git by default. Recreate models by running:

```bash
python -m cfllm.npe_seed --help
python -m cfllm.train_npe_seed --help
```
